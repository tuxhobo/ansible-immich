# inventories/hosts.yml

all:
  children:
    truenas:
      hosts:
        lala110:
          ansible_host: 10.0.0.110
          ansible_connection: local # API-only host; tasks use uri module against truenas_api_url
          ansible_python_interpreter: /usr/bin/python3

    docker_hosts:
      hosts:
        docker01:
          ansible_host: 10.0.0.115
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/ansible_pbs_key
